syntax = "proto2";

/**
* @brief config for polynomial reference trajectory
*/
message PolynomialReferenceConfig {
  /**
  * @brief kp
  */
  optional double max_velocity = 1 [ default = 0.5 ];
  optional double min_tf = 2 [ default = 1 ];
}
